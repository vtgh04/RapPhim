# Agent Skills Registry

This directory is reserved for local agent skills and automation workflow configurations.

---
## Purpose
Skills are reusable patterns or capabilities that the agent can load dynamically to speed up typical operations, such as:
- Test-driven development (TDD) setups
- Timezone database query checkers
- Style audits for Tailwind CSS v4 components
